# picamera2 NÃO entra aqui — instale via apt:
#   sudo apt install -y python3-picamera2
# e crie o venv com --system-site-packages (feito pelo setup.sh)

ultralytics>=8.3.0
supervision>=0.22.0
opencv-python-headless>=4.9.0
imagehash>=4.3.1
Pillow>=10.0.0
numpy>=1.26.0
requests>=2.31.0
